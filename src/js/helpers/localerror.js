'use strict';

class localError {

    constructor(name, msg) {
        
        let e = document.createElement("span");
        e.setAttribute("class", "error-item error-"+name);

        jQuery().html(label);

        return l;
    }

}

module.exports = localError;